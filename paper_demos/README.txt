File format:

<original c file>-<form of output>-<user bound>-<bound>.extension

User bound = the bound placed on loops contained within the actual target code
Bound = the bound placed on the loops used within the learn environment implementation